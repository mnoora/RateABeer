<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @beer.name %>
</p>

<p>
  <strong>Style:</strong>
  <%= @beer.style %>
</p>

<p>
  <strong>Brewery:</strong>
  <%= @beer.brewery.name %>
</p>

<% if @beer.ratings.empty? %>
 beer has not yet been rated!
<% else %>
 beer has <%= @beer.ratings.count %>  <%= 'rating'.pluralize(@beer.ratings.count)%>, average <%= @beer.average_rating %> 
<% end %>

</br>

<%= link_to 'Edit', edit_beer_path(@beer) if current_user%>
<br>
<%= link_to 'Destroy', @beer, method: :delete, data: { confirm: 'Are you sure?' } if current_user%>
